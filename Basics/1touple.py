

touple = ("chenchu","reddy","ram","bhim")  # Tuple items are ordered, unchangeable, and allow duplicate values.
print(touple)             #Tuples are unchangeable, meaning that we cannot change, add or remove items after the tuple has been created.

thistuple = ("apple", "banana", "cherry", "apple", "cherry")
print(thistuple)

print(len(thistuple))   # prints the length of the touple

touple1 =("chenchu",) # as I am putting a , after my element it's recognizing as the touble else it will consider as a string
print(type(touple1))
touple2 = ("chenchu")
print(type(touple2))

touple4 = ("chenchu", "reddy",1,2,3,4,True,False) #allow to store multiple datatypes in single touple
print(touple4)
touple5 = touple4 #assigning one to
print(touple5)




